# ChemFlow2 the revenge !

ChemFlow tools were developed sort of independenty it was time to reorganize.

# Original ChemFlow is at github
https://github.com/IFMlab/ChemFlow.git

#ChemFlow installation: 
export CHEMFLOW_HOME="PATH_TO_CHEMFLOW"
export PATH=${CHEMFLOW_HOME}/bin/:${PATH}

# That's it !

